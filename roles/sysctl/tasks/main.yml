- name: Configure sysctl parameters
  ansible.builtin.import_tasks: configure.yml

- name: Apply complete sysctl policy before AMI finalization
  ansible.builtin.meta: flush_handlers

- name: Validate sysctl STIG hardening configurations
  ansible.builtin.import_tasks: validate_stig.yml
